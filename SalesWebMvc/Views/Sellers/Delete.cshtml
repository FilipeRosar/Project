@model SalesWebMvc.Models.Seller

@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Are you sure you want to delete this?</h3>

<div>
    <h4>Seller</h4>
    <hr />
    <dl class="row">
        <div class="col-12 d-flex border-bottom py-2">
            <dt class="col-sm-3 mb-0">@Html.DisplayNameFor(Model => Model.Name)</dt>
            <dd class="col-sm-9 mb-0">@Html.DisplayFor(Model => Model.Name)</dd>
        </div>

        <div class="col-12 d-flex border-bottom py-2">
            <dt class="col-sm-3 mb-0">@Html.DisplayNameFor(Model => Model.Email)</dt>
            <dd class="col-sm-9 mb-0">@Html.DisplayFor(Model => Model.Email)</dd>
        </div>

        <div class="col-12 d-flex border-bottom py-2">
            <dt class="col-sm-3 mb-0">@Html.DisplayNameFor(Model => Model.BirthDate)</dt>
            <dd class="col-sm-9 mb-0">@Html.DisplayFor(Model => Model.BirthDate)</dd>
        </div>

        <div class="col-12 d-flex border-bottom py-2">
            <dt class="col-sm-3 mb-0">@Html.DisplayNameFor(Model => Model.BaseSalary)</dt>
            <dd class="col-sm-9 mb-0">@Html.DisplayFor(Model => Model.BaseSalary)</dd>
        </div>

        <div class="col-12 d-flex border-bottom py-2">
            <dt class="col-sm-3 mb-0">@Html.DisplayNameFor(Model => Model.Department)</dt>
            <dd class="col-sm-9 mb-0">@Html.DisplayFor(Model => Model.Department.Name)</dd>
        </div>
    </dl>


    <form asp-action="Delete">
        <input type="hidden" asp-for="Id">
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="Index">Back to List</a>
    </form>
</div>